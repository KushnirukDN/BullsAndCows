import java.awt.*;
import java.util.*;
import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

class Main {
    public static void main(String[] args) {
       /* Scanner scanner = new Scanner(System.in);
        String url = scanner.nextLine().substring(30);
        String[] urlToArray = url.split("&");
        String[] pass = urlToArray[0].split("=");
            result(urlToArray);
    }


    private static void result(String[] urlToArray) {
        boolean pass = false;
        String[] password = new String[2];
        for (int i = 0; i < urlToArray.length; i++) {
            String[] temp = urlToArray[i].split("=");
            if (temp[0].equals("pass")) {
                pass = true;
                password = temp;
            }
            if (temp.length == 2) {
                System.out.println(temp[0] + " : " + temp[1]);
            } else {
                System.out.println(temp[0] + " : " + "not found");
            }
        }
        if(pass == true) {
            System.out.println("password : " + password[1]);
        }*/

    }
}